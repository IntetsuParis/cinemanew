import styles from "./AccountHelper.module.scss";

const AccountHelper: React.FC = () => {
  return (
    <div className={styles.wrapper}>
      <h1>Add a movie to rate it 🦦</h1>
    </div>
  );
};

export default AccountHelper;
